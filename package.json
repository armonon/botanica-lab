{
  "name": "botanica-lab",
  "version": "0.2.0",
  "private": true,
  "description": "Botanica botanical R&D site with open-ended blend ideation chat.",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check api/chat.js && python3 -m json.tool data/research.json >/tmp/botanica-research.json"
  },
  "engines": {
    "node": ">=18"
  }
}
